"""LLM-as-judge evaluation harness for the L2 extraction agents.

Run with: docker compose exec backend python -m eval.run
(needs a working Kimi/Qwen backend, same as the app itself -- see
harness.py's module docstring for how scoring works.)
"""
